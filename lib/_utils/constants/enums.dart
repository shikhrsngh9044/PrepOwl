// ignore_for_file: constant_identifier_names, camel_case_types

enum Flavor {
  dev,
  staging,
  uat,
  prod,
}

enum ApiCallType {
  GET,
  POST,
  PUT,
  DELETE,
}

enum SIZES {
  extraSmall,
  small,
  medium,
  large,
  extraLarge,
}

enum AUTH_STATE {
  loggedIn,
  signedUp,
}
